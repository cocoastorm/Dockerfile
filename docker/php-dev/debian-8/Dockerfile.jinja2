{{ docker.from("php", "debian-8", user) }}

{{ docker.platformArgs() }}

{{ docker.copy('conf/', '/opt/docker/') }}

RUN set -x \
    {{ php5dev.debian() }} \
    {{ provision.runBootstrap() }} \
    {{ docker.cleanup() }}
