{{ docker.from("base", "ubuntu-15.10", user) }}

{{ docker.platformArgs() }}

{{ environment.baseApp() }}

{{ docker.copy('conf/', '/opt/docker/') }}

RUN set -x \
    {{ baseapp.ubuntu() }} \
    {{ provision.runBootstrap() }} \
    {{ docker.cleanup() }}
